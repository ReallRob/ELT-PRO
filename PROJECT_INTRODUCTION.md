# 项目介绍

## 项目概述

ELT-PRO 是一个面向 Excel、CSV 和 Excel 模板报表的桌面端数据处理工具。项目通过可视化工作流、参数化配置和独立运行器，把常见的数据清洗、汇总、连接、计算、模板填充和报表输出流程沉淀成可保存、可复用、可交付的 JSON 工作流。

项目主要包含两个程序：

- 主程序：可视化工作流设计器
- 独立程序：CRPA JSON 运行器

## 一、可视化工作流设计器

### 简要说明

可视化工作流设计器是面向流程设计人员、数据处理人员和开发配置人员的主程序。用户可以通过拖拽算子、连接节点、配置参数的方式搭建完整的数据处理流程，并将流程保存为工作流 JSON。

### 主要功能

- 可视化搭建数据处理流程，支持节点拖拽、连线和配置。
- 支持 Excel、CSV 数据导入和自动导出。
- 支持数据筛选、排序、清洗、去重、抽样、转置等常用数据变换。
- 支持分组汇总、数据透视、逆透视、描述统计等统计处理。
- 支持表连接、纵向拼接、排名、公式计算、累加和环比计算。
- 支持 Excel 模板加载、模板写入和模板保存。
- 支持代码块，对 DataFrame 和 Workbook 做自定义处理。
- 支持运行参数、参数映射、文件参数、文件夹参数和代码中的 `param()` 引用。
- 支持中间结果预览、节点运行测试和完整工作流保存。

### 优点

- 降低重复开发成本：常见数据处理逻辑可以通过配置完成，不需要每次重新写脚本。
- 流程可视化：数据从哪里来、经过哪些处理、输出到哪里都能在画布上直观看到。
- 可复用和可交付：流程可以保存为 JSON，在不同环境中重新加载和运行。
- 灵活扩展：普通规则处理不了的场景，可以通过代码块补充自定义逻辑。
- 更适合模板报表：支持在同一个 Workbook 主线上连续写入、加工并保存完整 Excel 模板。

### 解决的问题

- 解决 Excel/CSV 数据处理流程分散在手工操作、临时脚本和个人经验中的问题。
- 解决复杂报表流程难以复用、难以交接、难以排查的问题。
- 解决业务参数、文件路径、模板文件变动后需要反复修改代码的问题。
- 解决数据处理过程不可视、步骤不透明、中间结果难检查的问题。

## 二、CRPA JSON 运行器

### 简要说明

CRPA JSON 运行器是面向业务执行人员的独立运行程序。它不用于设计流程，而是用于加载已经设计好的工作流 JSON，根据运行清单自动生成文件选择和参数输入界面，然后一键执行流程。

### 主要功能

- 加载工作流 JSON 文件。
- 根据 `run_manifest` 自动生成运行表单。
- 支持重新选择数据源文件、模板文件和输出文件。
- 支持填写运行参数，包括文本、数字、日期、布尔、文件和文件夹参数。
- 支持数据源工作表映射和校验。
- 支持执行完整工作流并查看运行日志。
- 支持将新的文件路径、参数和工作表选择写回 JSON。
- 支持代码块超时控制，Workbook 代码块也可在独立进程中执行并按超时终止。

### 优点

- 使用门槛低：业务人员只需要选择 JSON、选择文件、填写参数并运行。
- 避免误改流程：运行器不暴露复杂设计能力，减少业务执行时误操作。
- 适合分发交付：设计人员配置好流程后，可以把 JSON 交给业务人员独立运行。
- 路径可迁移：不同电脑上的文件路径不同，运行器可以重新选择并写回。
- 执行更稳定：代码块支持超时限制，异常会在日志中暴露，减少流程长时间卡死。

### 解决的问题

- 解决流程设计人员和业务执行人员角色混在一起的问题。
- 解决业务人员运行自动化流程时还需要理解完整设计器的问题。
- 解决工作流换电脑、换文件路径后无法直接运行的问题。
- 解决每次运行都需要重新手工修改 JSON 或代码参数的问题。

## 总结

可视化工作流设计器负责“设计和沉淀流程”，CRPA JSON 运行器负责“交付和执行流程”。两者配合后，可以把原本依赖人工 Excel 操作或临时代码的数据处理工作，转化为可配置、可复用、可分发、可运行的标准化流程。
