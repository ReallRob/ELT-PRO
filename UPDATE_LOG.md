# 更新记录

## GitHub 上传状态

- 记录日期: 2026-07-08
- 目标分支: codex
- 当前状态: 未上传
- 标记规则: 只有确认本地最新代码已经成功 push 到 GitHub 后，才把当前状态改为“已上传”，并补充提交信息。

## 本次更新摘要

本次更新围绕“数据排名 UI、加载模板预览、代码块执行、多线程运行、缓存复用、关闭稳定性”进行整改。重点是减少 UI 线程里的重 IO 和重计算，避免代码块串联时重复执行上游，并让模板、代码块和预览区域的数据状态更稳定。

## 关键改动

- 数据排名 UI 改为中文显示：选择列和输入排名名称对半展示，排序方式和排名名次各自独立一行，默认排序为降序，默认排名名次为不跳号。
- 未运行的加载模板节点不再因为数据预览临时触发 `load_workbook(path)`，避免选中节点或预览时反复读取模板文件。
- 加载模板点击浏览文件时不再提前扫描工作表，运行当前节点时才读取模板文件，避免浏览一次、运行一次的重复加载。
- 加载模板预览参数改为 4 行纵向输入：起始行、起始列、最大行数、最大列数；移除预览工作表和预览表个数配置。
- 数据预览区域支持从原始结果缓存读取 Workbook，并按所有 sheet 展示，不再因为切换节点丢失预览。
- 下游节点不再因为点击或切换上游节点就直接清空数据；上游重新运行后，相关连线显示脏数据状态，用红色虚线提示。
- 代码块接代码块时，后续节点优先复用干净的上游原始输出缓存，避免把前一个代码块重复执行一遍。
- Workbook 原始缓存注入下游前会进行克隆，降低下游修改污染上游缓存的风险。
- 代码块支持实时日志，`print()` 和 stderr 可进入日志流，执行模式和代码块面板都能看到运行输出。
- Workbook/Worksheet 类型输入改为后台线程执行，避免“子进程返回 wb -> 保存临时 xlsx -> 父进程再 load_workbook”的重 IO 流程。
- DataFrame/普通代码块继续使用子进程执行，便于超时后强制终止；Workbook 相关路径使用超时废弃和取消检查控制结果回写。
- 设计态单节点运行改为通过后台 `WorkflowEngine` 执行，减少长时间同步运行阻塞 UI 的情况。
- Excel 数据源 sheet 扫描和模板元数据扫描移到后台线程，减少导入数据源、选择模板文件时的短暂停顿。
- 保存模板相关的 `wb.save()` 路径统一交给后台 `WorkflowEngine`，避免直接在 UI 线程保存大文件。
- 修复关闭程序时可能出现 `0xC0000005` 的问题：关闭时停止运行任务、清理预览模型和画布场景，再退出 Qt 主循环。

## 验证记录

- 已通过核心 Python 文件的 `py_compile` 编译检查。
- 已执行 `git diff --check`，仅出现 CRLF 换行提示。
- 已验证自动加载真实工作流后关闭程序，退出码为 `0`。
- 已做代码块 DataFrame 子进程运行、实时日志、Workbook 上游内存执行、代码自身创建 Workbook 返回、上游原始缓存复用等烟测。

## 后续注意事项

- UI 线程不应直接执行 `pandas.read_excel`、`openpyxl.load_workbook`、`workbook_to_preview_data`、`wb.save` 或用户代码。
- 代码块如果自己调用 `load_workbook`，读取应发生在代码块执行环境中，不应回到 UI 线程执行。
- 大 Workbook 的预览继续遵循按需、限量、缓存优先原则，避免全量转换。
- 上传 GitHub 前应再次确认 `git status`，避免把运行态文件、本地配置或临时文件误提交。
